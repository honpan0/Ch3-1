#include <stdio.h> 
#include <stdlib.h> 
#include <math.h>
int main(void)
{
int X;
for（×=1;×<=10;x++)
  printf("%.2f ",sqrt(x));
printf("\n");
system ("pause");
return 0;
}#include <stdio.h> 
#include <stdlib.h> 
#include <math.h>
int main(void)
{
int X;
for（×=1;×<=10;x++)
  printf("%.2f ",sqrt(x));
printf("\n");
system ("pause");
return 0;
}
